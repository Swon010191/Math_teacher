# Cấu hình AI cho Recognition thật (giai đoạn sau)
# Stage 1 dùng MockRecognitionProvider, không cần cấu hình gì.

# Loại provider: mock | ollama_vision | pix2text
RECOGNITION_PROVIDER=mock

# Ollama (khi dùng OllamaVisionProvider)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llava